Code Breakdown1. InitializationJavaMap<String, List<String>> map = new HashMap<>();
We create a HashMap named map.Key (String): This will store the sorted version of a word. The sorted version acts as a unique identifier (or signature) for an anagram group. For example, "eat", "tea", and "ate" will all point to the key "aet".Value (List<String>): A list that stores all the original words that share that same sorted signature.2. Iterating Through the ArrayJavafor (String s : strs) {
We use an enhanced for loop to look at each individual string s in the input array strs one by one.3. Creating the Anagram SignatureJavachar[] charArray = s.toCharArray();
Arrays.sort(charArray);
String sortedStr = new String(charArray);
s.toCharArray(): Java strings are immutable, so we convert the string into an array of characters so we can manipulate it.Arrays.sort(charArray): We sort the character array alphabetically. This is the crucial step because any words that are anagrams of each other will become completely identical once sorted.new String(charArray): We convert the sorted character array back into a standard Java String object (sortedStr) so it can be used as a key in our map.4. Grouping in the MapJavaif (!map.containsKey(sortedStr)) {
    map.put(sortedStr, new ArrayList<>());
}
map.get(sortedStr).add(s);
if (!map.containsKey(...)): We check if this sorted signature has been seen before. If it's a brand-new signature, we initialize a new empty ArrayList for it in the map.map.get(sortedStr).add(s): We fetch the list associated with that signature and append the original string s to it.5. Returning the ResultJavareturn new ArrayList<>(map.values());
map.values() returns a collection of all the lists we created (e.g., [[bat], [nat, tan], [ate, eat, tea]]).We wrap map.values() inside new ArrayList<>(...) to convert that collection into the required List<List<String>> return format.Complexity AnalysisLet:$N$ be the total number of strings in the input array strs.$K$ be the maximum length of a single string in strs.Time Complexity: $O(N \cdot K \log K)$Looping: We iterate through the array of strings exactly once, which takes $O(N)$ time.Sorting: Inside the loop, for every single string, we sort its characters. Sorting a string of maximum length $K$ takes $O(K \log K)$ time.Map Operations: Inserting and looking up keys in a HashMap takes $O(K)$ time on average because the system has to hash the string key of length $K$.Total Time: Combining the loop and the sorting/handling inside it gives $O(N \cdot (K \log K + K))$, which simplifies to $O(N \cdot K \log K)$.Space Complexity: $O(N \cdot K)$Map Storage: In the worst-case scenario (where no two words are anagrams), the map will store every single string from the input. There are $N$ strings, each taking up to $K$ space, leading to $O(N \cdot K)$ space.Character Arrays: Inside the loop, creating the temporary charArray takes $O(K)$ space, but this memory is freed up in every iteration.Total Space: $O(N \cdot K)$ to store the grouped results inside the map.
